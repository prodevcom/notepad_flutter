name: notepad
description: Projeto acadêmico de bloco de notas para o MBA de Desenvolvimento Mobile da Fiap

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^2.5.0
  firebase_auth: ^4.2.6
  font_awesome_flutter: ^10.3.0
  cloud_firestore: ^4.3.2
  intl: ^0.17.0
  firebase_crashlytics: ^3.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.11.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/laucher_icon.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
